random code

@bot.listen("on_message")
async def printmsgs(ctx):
 time = datetime.now()
 current_time = time.strftime("%H:%M:%S")
 print(f"{ctx.author} sent: {ctx.content} at {current_time},the server is {ctx.guild.name} and the channel is #{ctx.channel.name}")

@bot.before_invoke
async def printcmdsintoconsole(ctx):
 time = datetime.now()
 current_time = time.strftime("%H:%M:%S")
 print(f"{ctx.author} used {ctx.command} at {current_time}")
 

@bot.listen("on_message_edit")
async def printedits(ctx, before):
 time = datetime.now()
 current_time = time.strftime("%H:%M:%S")
 print(f"{ctx.author} edited their message from {ctx.content} to: {before.content} at {current_time}")